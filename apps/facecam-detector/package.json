{
  "name": "facecam-detector",
  "version": "1.0.0",
  "scripts": {
    "dev": "STAGE=dev uvicorn main:app --reload --port 8000",
    "prod": "STAGE=production uvicorn main:app --host 0.0.0.0 --port 8000",
    "install:python": "python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt"
  },
  "dependencies": {
    "turbo": "latest"
  }
}
