{
  "name": "facecam-detector",
  "version": "1.0.0",
  "scripts": {
    "dev": "cross-env STAGE=dev uv run --project . uvicorn main:app --reload --port 8000",
    "prod": "cross-env STAGE=production poetry run uvicorn main:app --host 0.0.0.0 --port 8000",
    "install:python": "poetry install"
  },
  "dependencies": {
    "turbo": "latest"
  }
}
