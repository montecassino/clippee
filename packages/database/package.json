{
  "name": "@repo/database",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@prisma/adapter-mariadb": "^7.2.0",
    "@prisma/client": "^7.2.0"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "prisma": "latest",
    "typescript": "latest"
  },
  "scripts": {
    "db:dev:push": "cross-env NODE_ENV=dev prisma db push",
    "db:studio": "prisma studio",
    "db:dev:generate": "cross-env NODE_ENV=dev prisma generate"
  }
}
