{
  "name": "@database/prisma",
  "version": "0.0.1",
  "scripts": {
    "db:dev:push": "NODE_ENV=dev prisma db push", 
    "db:studio": "prisma studio",
    "db:dev:generate": "NODE_ENV=dev prisma generate"
  }
}
